# Copy to .env for local development (never commit .env).
# On Netlify, set the same variable in Site settings → Environment variables.
# Leave the value blank here; fill it only in .env or the Netlify UI.
SITE_PASSWORD=
